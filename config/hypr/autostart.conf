# Hyprland Autostart Programs

# Start waybar
exec-once = waybar

# Start notification daemon
exec-once = mako

# Start clipboard manager (cliphist with wl-paste watching)
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Start idle management
exec-once = hypridle

# Start nightlight (hyprsunset)
exec-once = hyprsunset

# Start walker service (app launcher)
exec-once = walker --gapplication-service

# Add any personal autostart programs below
